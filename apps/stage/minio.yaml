image:
  name: bitnami/minio
  tag: 2025.2.18-debian-12-r2
ports:
  s3: 9000
  storage: 9001
env:
  MINIO_ROOT_USER: admin
  MINIO_DEFAULT_BUCKETS: bucket:public
secrets:
  minio-secret:
    MINIO_ROOT_PASSWORD: password